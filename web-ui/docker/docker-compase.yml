services:
  web-ui:
    image: nginx:alpine
    container_name: forged-web-ui
    ports:
      - "8080:80"
    volumes:
      - ../app:/usr/share/nginx/html:ro
      - ../nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
